Only in /home/ad6224sh: .apptainer
Only in /home/ad6224sh: Assignment 1
Only in /home/ad6224sh: .bash_history
diff -y /etc/skel/.bash_logout /home/ad6224sh/.bash_logout
# ~/.bash_logout						# ~/.bash_logout

Only in /home/ad6224sh: .bash_logout~
diff -y /etc/skel/.bash_profile /home/ad6224sh/.bash_profile
# .bash_profile							# .bash_profile

# Get the aliases and functions					# Get the aliases and functions
if [ -f ~/.bashrc ]; then					if [ -f ~/.bashrc ]; then
	. ~/.bashrc							. ~/.bashrc
fi								fi

# User specific environment and startup programs		# User specific environment and startup programs
							      >
							      >	export PATH="/projects/hep/fs11/mxnb11/bin:$PATH"
Only in /home/ad6224sh: .bash_profile~
diff -y /etc/skel/.bashrc /home/ad6224sh/.bashrc
# .bashrc							# .bashrc

# Source global definitions					# Source global definitions
if [ -f /etc/bashrc ]; then					if [ -f /etc/bashrc ]; then
	. /etc/bashrc							. /etc/bashrc
fi								fi

# User specific environment					# User specific environment
if ! [[ "$PATH" =~ "$HOME/.local/bin:$HOME/bin:" ]]		if ! [[ "$PATH" =~ "$HOME/.local/bin:$HOME/bin:" ]]
then								then
    PATH="$HOME/.local/bin:$HOME/bin:$PATH"			    PATH="$HOME/.local/bin:$HOME/bin:$PATH"
fi								fi
export PATH							export PATH

# Uncomment the following line if you don't like systemctl's 	# Uncomment the following line if you don't like systemctl's 
# export SYSTEMD_PAGER=						# export SYSTEMD_PAGER=

# User specific aliases and functions				# User specific aliases and functions
if [ -d ~/.bashrc.d ]; then					if [ -d ~/.bashrc.d ]; then
	for rc in ~/.bashrc.d/*; do					for rc in ~/.bashrc.d/*; do
		if [ -f "$rc" ]; then						if [ -f "$rc" ]; then
			. "$rc"								. "$rc"
		fi								fi
	done								done
fi								fi

unset rc							unset rc
Only in /home/ad6224sh: .bashrc~
Only in /home/ad6224sh: .cache
Only in /home/ad6224sh: .config
Only in /home/ad6224sh: Desktop
Only in /home/ad6224sh: Documents
Only in /home/ad6224sh: Downloads
diff -y /etc/skel/.emacs /home/ad6224sh/.emacs
;; .emacs							;; .emacs

(custom-set-variables						(custom-set-variables
 ;; uncomment to always end a file with a newline		 ;; uncomment to always end a file with a newline
 ;'(require-final-newline t)					 ;'(require-final-newline t)
 ;; uncomment to disable loading of "default.el" at startup	 ;; uncomment to disable loading of "default.el" at startup
 ;'(inhibit-default-init t)					 ;'(inhibit-default-init t)
 ;; default to unified diffs					 ;; default to unified diffs
 '(diff-switches "-u"))						 '(diff-switches "-u"))

;;; uncomment for CJK utf-8 support for non-Asian users		;;; uncomment for CJK utf-8 support for non-Asian users
;; (require 'un-define)						;; (require 'un-define)
Only in /home/ad6224sh: .emacs~
Only in /home/ad6224sh: git
Only in /home/ad6224sh: .gitconfig
Only in /home/ad6224sh: .history
diff -y /etc/skel/.kshrc /home/ad6224sh/.kshrc
# .kshrc							# .kshrc

# Source global definitions					# Source global definitions
if [ -f /etc/kshrc ]; then					if [ -f /etc/kshrc ]; then
	. /etc/kshrc							. /etc/kshrc
fi								fi

# use emacs editing mode by default				# use emacs editing mode by default
set -o emacs							set -o emacs

# User specific aliases and functions				# User specific aliases and functions

Only in /home/ad6224sh: .kshrc~
Only in /home/ad6224sh: .lesshst
Only in /home/ad6224sh: .local
Common subdirectories: /etc/skel/.mozilla and /home/ad6224sh/.mozilla
Only in /home/ad6224sh: multicore.sh
Only in /home/ad6224sh: Music
Only in /home/ad6224sh: .nv
Only in /home/ad6224sh: Pictures
Only in /home/ad6224sh: .pki
Only in /home/ad6224sh: Public
Only in /home/ad6224sh: simplejob.sh
Only in /home/ad6224sh: .ssh
Only in /home/ad6224sh: Templates
Only in /home/ad6224sh: TOP-MNXB11
Only in /home/ad6224sh: Videos
Only in /home/ad6224sh: .viminfo
Only in /home/ad6224sh: .vscode
Only in /home/ad6224sh: .wget-hsts
diff -y /etc/skel/.zshrc /home/ad6224sh/.zshrc
#								#
# .zshrc is sourced in interactive shells.			# .zshrc is sourced in interactive shells.
# It should contain commands to set up aliases,			# It should contain commands to set up aliases,
# functions, options, key bindings, etc.			# functions, options, key bindings, etc.
#								#

autoload -U compinit						autoload -U compinit
compinit							compinit

#allow tab completion in the middle of a word			#allow tab completion in the middle of a word
setopt COMPLETE_IN_WORD						setopt COMPLETE_IN_WORD

## keep background processes at full speed			## keep background processes at full speed
#setopt NOBGNICE						#setopt NOBGNICE
## restart running processes on exit				## restart running processes on exit
#setopt HUP							#setopt HUP

## history							## history
#setopt APPEND_HISTORY						#setopt APPEND_HISTORY
## for sharing history between zsh processes			## for sharing history between zsh processes
#setopt INC_APPEND_HISTORY					#setopt INC_APPEND_HISTORY
#setopt SHARE_HISTORY						#setopt SHARE_HISTORY

## never ever beep ever						## never ever beep ever
#setopt NO_BEEP							#setopt NO_BEEP

## automatically decide when to page a list of completions	## automatically decide when to page a list of completions
#LISTMAX=0							#LISTMAX=0

## disable mail checking					## disable mail checking
#MAILCHECK=0							#MAILCHECK=0

# autoload -U colors						# autoload -U colors
#colors								#colors
Only in /home/ad6224sh: .zshrc~
